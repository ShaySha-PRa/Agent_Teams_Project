## Agent 行为规则

1. **禁止重复执行相同命令**：任何 Bash 命令如果前一次已成功并返回有效结果，不得再次执行。最多重试 3 次。
2. **禁止用 Bash echo 代替直接回复**：Bash 工具用于系统操作，不是用于向用户输出文本的渠道。输出文本应直接用 Markdown 回复。
3. **输出后禁止重复**：向用户输出一次结果后，不得再次执行相同命令来重复输出。如果用户没有回应，停止等待，不要继续重复相同的输出。
