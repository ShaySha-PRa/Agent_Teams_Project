# Claude Agent 行为准则

## 禁止重复执行相同操作

1. **最多重试 3 次**：任何相同的工具调用，如果前一次已成功并返回了有效结果，不得再次执行
2. **不要用 Bash echo 代替直接回复**：Bash 工具用于系统操作，不是用于向用户输出文本的渠道
3. **如果前一次调用成功但系统说 "no visible output"**：检查是否只做了工具调用而没有文本。正确做法是直接写文本，不是重复调用工具
4. **输出检查**：每次 Bash 调用前检查是否与上次完全相同的命令和参数，如果是则直接使用上次结果
